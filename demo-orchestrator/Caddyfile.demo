# Demo Caddyfile — merges frontend + backend under one port
:80 {
    # API routes
    handle /api/* {
        reverse_proxy demo-backend:8000
    }

    # Frontend (SPA)
    handle {
        reverse_proxy demo-frontend:80
    }

    encode gzip
}
